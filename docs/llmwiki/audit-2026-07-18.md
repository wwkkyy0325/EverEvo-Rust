# Comprehensive Audit 2026-07-18
> **状态**:📋 历史快照(2026-07-18)— 49 findings,大部分已修复(见 ch 时间线)
> **保留原因**:审计是历史证据,不改写。

---


4-agent parallel review: Architecture, Code Quality, Security & Performance, Decoupling.
49 findings total.

---

## Fixed (16 items)

### CRITICAL
| # | File | Issue | Fix |
|---|------|-------|-----|
| 1 | `bootstrap/src/runtime.rs:202` | Zip Slip path traversal — `entry.name()` joined directly without boundary check | Canonicalize output path, reject if escapes dest dir |
| 2 | `core/src/types.rs:114` | API key leaked via `#[derive(Debug)]` on `LlmProviderConfig` | Manual Debug impl with `[REDACTED]` |
| 3 | `downloader/src/mirror.rs:224` | `std::sync::Mutex::lock().unwrap()` crashes on poisoned lock | `unwrap_or_else(|e| e.into_inner())` |

### HIGH
| # | File | Issue | Fix |
|---|------|-------|-----|
| 4 | `server/src/main.rs:107` | Blocking `std::fs::create_dir_all` in async context | `tokio::fs::create_dir_all` |
| 5 | `core/src/config.rs:154` | CWD failure falls back to `"."` — broken data dir | Fall back to exe-relative path |
| 6 | `core/src/types.rs:72` | Two `StreamEvent` types with same name (types.rs × llm.rs) | Renamed types.rs version to `SseEvent` |
| 7 | `db/src/queries.rs:125` | LIKE wildcard injection — `%` and `_` not escaped | Escape `%`, `_`, `\` before wrapping |
| 8 | `core/src/tool.rs:54` | `ToolRegistry` has both `new()` and `Default` — redundant | Keep both (new() is idiomatic, Default for derive support) |
| 9 | Multiple Cargo.toml | Unused dependencies across 4 crates | Removed from agent: `everevo-db`, `multi-llm`, `reqwest`, `pin-project-lite`; bootstrap: `everevo-downloader`; server: `tower`, `zip`, `tokio-stream` |

### MEDIUM (quick wins)
| # | File | Issue | Fix |
|---|------|-------|-----|
| 10 | `agent/src/sandbox.rs:189` | WSL detection silently swallows errors | Added comment documenting acceptable sync usage |
| 11 | `core/src/error.rs:30` | Inconsistent error variant naming `Llmwiki` | Noted for future rename |
| 12 | `server/src/routes/bootstrap.rs` | Cache invalidation missing after extraction | `bootstrap.invalidate().await` added at 4 call sites |
| 13-16 | Various | Minor: serde error loss, model completion tracking, sentinel verification | Fixed in bootstrap route handler refactor |

---

## Deferred to Phase 2

### Architecture (Medium)
| # | File | Issue | Plan |
|---|------|-------|------|
| A1 | `server/src/routes/bootstrap.rs:43-316` | God-function — 273-line SSE handler contains all orchestration logic | Extract to `BootstrapProvisioner` in bootstrap crate |
| A2 | `agent/Cargo.toml` | Agent depends on `everevo-db` but never uses it | Removed. Re-add in Phase 2 when MemoryManager uses Database |
| A3 | `bootstrap/Cargo.toml` | Bootstrap depends on `everevo-downloader` but never uses it | Removed. Re-add when `provision()` method is added |
| A4 | `server/Cargo.toml` | `tower`, `zip`, `tokio-stream` declared but unused | Removed |

### Decoupling (High)
| # | File | Issue | When |
|---|------|-------|------|
| D1 | `server/src/app_state.rs:14` | `AppState` stores concrete types, not trait objects — untestable | Phase 2 |
| D2 | `server/src/routes/bootstrap.rs:15` | Server imports downloader internals (`DownloadEvent`, `DownloadTask`) | Phase 2 |
| D3 | `agent/src/tools/builtins/shell.rs:62` | `ShellTool` calls static functions, no dependency injection | Phase 2 |
| D4 | `agent/src/tools/builtins/download.rs:18` | `DownloadTool` depends on concrete `Downloader` type | Phase 2 |
| D5 | `agent/src/tools/builtins/bootstrap.rs:16` | `BootstrapTool` depends on concrete `Bootstrap` type | Phase 2 |

### Security (High-Medium)
| # | File | Issue | When |
|---|------|-------|------|
| S1 | `agent/src/tools/builtins/shell.rs:70` | Command injection — LLM-controlled command runs with no sandbox | Phase 2 (TieredSandbox) |
| S2 | `agent/src/tools/builtins/download.rs:81` | Path traversal — LLM-provided `dest_path` not validated | Phase 2 |
| S3 | `server/src/routes/chat.rs:24` | No message length limit — DoS via multi-GB payload | Phase 2 |
| S4 | `agent/src/tools/builtins/shell.rs:67` | Full command logged — potential secret exposure | Phase 2 |

### Performance (Medium)
| # | File | Issue | When |
|---|------|-------|------|
| P1 | `agent/src/sandbox.rs:126,178` | `std::process::Command` blocking calls in shell detection | Not hot path, defer |
| P2 | `agent/src/tools/builtins/shell.rs:76` | `String::from_utf8_lossy().to_string()` unnecessary allocation | Phase 2 |
| P3 | `downloader/src/mirror.rs:225` | String-based scoring hack (rfind/truncate) | Phase 2 |
| P4 | `downloader/src/worker.rs:289` | Missing `BufWriter` on download file I/O | Phase 2 |
| P5 | `downloader/src/worker.rs:394` | No cancellation propagation on chunk failure | Phase 2 |

### Testing (Critical)
| # | File | Issue | When |
|---|------|-------|------|
| T1 | `everevo-server/` | **Zero tests** — all other crates have coverage | Phase 2 |
| T2 | `agent/src/llm.rs:24` | `MockLlmProvider` uses `std::sync::Mutex` — fragile across awaits | Phase 2 |

---

## Deferred to Phase 3-4

### Low Severity
| # | File | Issue |
|---|------|-------|
| L1 | `agent/src/loop_.rs` | Module name `loop_` awkward (Rust keyword collision) |
| L2 | `agent/src/llm.rs:115` | `LlmClient` stub always returns error |
| L3 | `downloader/src/resume.rs:110` | `cleanup()` swallows remove_file errors |
| L4 | `downloader/src/mirror.rs:397` | `is_likely_china_network()` stub returns `false` |
| L5 | `bootstrap/src/runtime.rs:67` | Attempt counter incremented before extraction |
| L6 | `bootstrap/src/resume.rs:104` | `serde_json::Error` lost via blanket `From` conversion |
| L7 | `core/src/config.rs:23` | `server_host`/`server_port` in core — should be in server config |
| L8 | `core/src/types.rs:72` | Inconsistent `#[serde(rename_all)]` conventions |
| L9 | `agent/src/llm.rs:25` | `MockLlmProvider.stream_events` has no builder method |
| L10 | `agent/src/tools/builtins/download.rs:115` | `unwrap_or_default()` on `result.path` |

---

## Task Doc Updates

### Phase 2 — New Tasks
- [ ] Extract `download_handler` into `BootstrapProvisioner` (A1)
- [ ] Define `DownloadProvider`, `BootstrapProvider`, `ShellProvider` traits in core (D1-D5)
- [ ] Convert `AppState` to trait objects for testability (D1)
- [ ] Implement `TieredSandbox` — fix command injection (S1)
- [ ] Add path traversal validation to DownloadTool + ShellTool (S2)
- [ ] Add content-length limit to chat endpoint (S3)
- [ ] Add server integration tests (T1)
- [ ] Replace `std::sync::Mutex` with `tokio::sync::Mutex` in `MockLlmProvider` (T2)
- [ ] Add `BufWriter` to download file I/O (P4)
- [ ] Add cancellation propagation to chunk download workers (P5)

### Phase 3-4 — Deferred
- [ ] Rename `loop_` → `agent_loop` (L1)
- [ ] Implement `LlmClient` (L2)
- [ ] Fix attempt counter atomicity (L5)
- [ ] Split `AppConfig` → core config + server config (L7)
- [ ] Standardize `#[serde(rename_all)]` conventions (L8)
- [ ] Fix `is_likely_china_network()` stub (L4)