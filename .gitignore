# ── Rust ──
target/
**/*.rs.bk

# ── Data (runtime, created on first launch) ──
data/

# ── Bundled assets (generated by scripts/bundle-assets.ps1) ──
resources/bundled/**
!resources/bundled/.gitkeep

# ── EverEvo internals ──
.everevo/

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo

# ── OS ──
.DS_Store
Thumbs.db

# ── Node / Frontend ──
node_modules/
frontend/dist/
frontend/.vite/
*.tsbuildinfo

# ── Tauri generated ──
src-tauri/gen/
src-tauri/session/

# ── Environment ──
.env
.env.local
*.env

# ── Misc ──
*.log
tmp/
sample-project/
*.bak
target-linux/
data-pre-gaia/

# ── Python (benchmark harnesses) ──
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
__pycache__/
*.py[cod]

# ── Compiled build artifacts ──
scripts/gaia-docker/everevo-server

# ── Harbor / Terminal-Bench (WS4, 2026-08-13) ──
# Downloaded TB-2.0 dataset (89 tasks, ~46M)
terminal-bench/
# Harbor run output + cache
jobs/
.harbor/
# Docker cross-compile cargo cache
.cargo-docker/

# ── Stray path-conversion artifacts (Git Bash) ──
# Accidental mount root from MSYS path mangling
C:/

# ── GAIA agent run artifacts (sandbox writes to repo root) ──
# Raw page/data dumps + verification leftovers the GAIA agent writes during
# benchmark runs. Some contain ground-truth-derived content — never commit.
nature*.html
nature*.json
nagel_*.txt
orcid_results*.txt
final_counts.txt
final_verify.txt
all_analysis.txt
b_analysis.txt
b_conflicts.txt
b_definitive.txt
b_noyear*.txt
os1.xml
sample3.json
count_nature.py